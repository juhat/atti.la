---
layout: default
---

<div class="container mx-auto flex flex-wrap p-5 flex-col md:flex-row items-center">
    <article class="prose lg:prose-lg mt-6 mb-0 pb-12 border-b border-gray-400">
        <h1>{{ page.title }}</h1>
        {{ content }}
    </article>
    {% render "about", about: site.data.coaching.about %}
</div>
